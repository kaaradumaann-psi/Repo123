# optik-form.html is the single-file deliverable and is intentionally tracked so it can be
# handed over without a build step. It is regenerated by `npm run build` inside mmpi-optik-formu
# and tests/build.test.ts fails if the embedded form PDF drifts from the committed one.
